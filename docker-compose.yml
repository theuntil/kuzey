version: "3.9"

services:
  web:
    container_name: kuzeybatihaber
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
    restart: unless-stopped
